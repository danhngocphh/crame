const isValidate = require('./isValidate');
const isAuth = require('./isAuth');
const attachPaginateOptions = require('./attachPaginateOptions');

module.exports = {
  isValidate,
  isAuth,
  attachPaginateOptions
};
